import { ImageuploadDirective } from './imageupload.directive';

describe('ImageuploadDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageuploadDirective();
    expect(directive).toBeTruthy();
  });
});
